# Workspace Rules

- Do NOT execute `git commit` or `git push` automatically.
- After completing file edits or feature implementations, stop and leave modified files for the user to review and commit manually via the Source Control tab.
